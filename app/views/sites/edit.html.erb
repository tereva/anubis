 <!-- Page Heading/Breadcrumbs -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Modification d'un site
                </h1>
                <ol class="breadcrumb">
                    <li>
                    <%= link_to 'Home', home_path %>
                    </li>
                    <li class="active"><%= @site.title %></li>
                </ol>
            </div>
        </div>
        <!-- /.row -->


<p id="notice"><%= notice %></p>

<%= render 'form', site: @site %>

<%= link_to 'Back', home_path %>
